import groovy.json.JsonSlurper

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('JiraUpdate') {
            steps {
            	
            	script{
            
               		issueKey = "CAL-13" // replace with the actual issue key
	            }
            }
        }
    }
}
